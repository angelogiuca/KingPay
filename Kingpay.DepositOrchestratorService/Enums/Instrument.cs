namespace Kingpay.DepositOrchestratorService.Enums
{
    /// <summary>
    /// Currently an enum, but ideally should be from db
    /// </summary>
    public enum Instrument
    {
        Offline = 0,
        MoMo = 1,
        Card = 2
    }
}
